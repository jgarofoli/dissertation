% $Id: aliases.tex 28 2010-10-05 15:19:02Z tskwarni $
% ===============================================================================
% Purpose: use of predefined symbols in the standard template
% Author: Tomasz Skwarnicki
% Created on: 2010-09-24
% ===============================================================================

\def\dzdzb{{~\raise.85em\hbox{{\tiny{(}\textemdash\tiny{)}}}\kern-1.05em
          \lower0.0em\hbox{$D^0$}~}\xspace}
\def\bsbsb{{~\raise.85em\hbox{{\tiny{(}\textemdash\tiny{)}}}\kern-1.05em
          \lower0.0em\hbox{$B_s^0$}~}\xspace}



\def\updateme{{\large \bfseries [UPDATE ME]}}
\def\nsig{N_{\rm sig}}
\def\nback{N_{\rm back}}
\def\bb{b\bar{b}}
\def\bs{B_s}
\def\bsb{\bar{B}_s}
\def\dsp{D_s^+}
\def\dsm{D_s^-}
\def\dspm{D_s^{\pm}}
\def\lam{\lambda}
\def\lamp{\lambda^{\prime}}
\def\bsdsk{\bsbsb\to D_s^{\mp} K^{\pm}}
\def\bsdsstark{B_s\to D_s^{\mp*} K^{\pm}}
\def\bsdskst{B_s\to D_s^{\mp} K^{*\pm}, K^{*\pm}\to K_s\pi^+}
\def\bsdskpi{B_s\to D_s^{\mp} K^{*\pm}, K^{*\pm}\to K^+\pi^0}
\def\bsdsp{B_s\to D_s^{\mp} \pi^{\pm}}
\def\bsdskpp{B_s\to D_s^{\mp} K^{\pm}\pi^{\mp}\pi^{\pm}}
\def\bsdskppa{B_s\to D_s^{-} (K^+\pi^-\pi^+)}
\def\bsdskppa{\bar{B}_s\to D_s^{-} (K^+\pi^-\pi^+)}
\def\bsbdsk{B_s\to D_s^{\pm} K^{\mp}}
\def\dg{\Delta\Gamma_s}
%\def\dms{\Delta m_s}
\def\terma{(1+|\lam_f|^2)\cosh(\dg t/2)}
\def\termb{(1-|\lam_f|^2)\cos (\dms t)}
\def\termadspi{\cosh(\dg t/2)}
\def\termbdspi{\cos (\dms t)}
\def\termad{(1+|\lam_f(\dd)|^2)\cosh(\dg t/2)}
\def\termbd{(1-|\lam_f(\dd)|^2)\cos (\dms t)}
\def\termae{(1+|\lam_f^{eff}|^2)\cosh(\dg t/2)}
\def\termbe{(1-|\lam_f^{eff}|^2)\cos (\dms t)}
\def\termcp{|\lam_f|\cos(\delta+\gamma)\sinh(\dg t/2)}
\def\termcn{|\lam_f|\cos(\delta-\gamma)\sinh(\dg t/2)}
\def\termdp{|\lam_f|\sin(\delta+\gamma)\sin(\dms t)}
\def\termdn{|\lam_f|\sin(\delta-\gamma)\sin(\dms t)}
\def\termcpd{2|\lam_f(\dd)|\cos(\delta(\dd)+\gamma)\sinh(\dg t/2)}
\def\termcnd{2|\lam_f(\dd)|\cos(\delta(\dd)-\gamma)\sinh(\dg t/2)}
\def\termdpd{2|\lam_f(\dd)|\sin(\delta(\dd)+\gamma)\sin(\dms t)}
\def\termdnd{2|\lam_f(\dd)|\sin(\delta(\dd)-\gamma)\sin(\dms t)}
\def\termcpe{2|\lam_f^{\prime}|\cos(\delta^{eff}+\gamma)\sin(\dg t/2)}
\def\termcne{2|\lam_f^{\prime}|\cos(\delta^{eff}-\gamma)\sin(\dg t/2)}
\def\termdpe{2|\lam_f^{\prime}|\sin(\delta^{eff}+\gamma)\sin(\dms t)}
\def\termdne{2|\lam_f^{\prime}|\sin(\delta^{eff}-\gamma)\sin(\dms t)}
\def\Aeffsq{\int{|A_f(\dd)|^2d\dd}}
\def\dd{{\cal{D}}}
\def\br{{\cal{B}}}



%\def\xbtoxcpipipi{X_b\to X_c\pi^-\pi^+\pi^-}
%\def\xbtoxcpi{X_b\to X_c\pi^-}
%\def\xbtoxckpipi{X_b\to X_cK^-\pi^+\pi^-}
%\def\xbtoxck{X_b\to X_cK^-}

\def\xb{H_b}
\def\xc{H_c}
\def\Lb{\Lambda_b^0}
\def\Lc{\Lambda_c^+}
\def\Sc{\Sigma_c}
%\def\bs{\bar{B}_s^0}
%\def\bz{\bar{B}^0}
%\def\bm{B^-}
%\def\dz{D^0}
%\def\ds{D_s^+}
%\def\dp{D^+}

\def\pimm{\pi{\raisebox{2.5pt}{$\scriptstyle-$}}{\mspace{-11mu}\raisebox{5.25pt}{$\scriptstyle-$}}}

\def\xbtoxchhh{\xb\to\xc h^-h^+h^-}
\def\xbtoxcpipipi{\xb\to\xc\pi^-\pi^+\pi^-}
\def\xbtoxcpi{\xb\to \xc\pi^-}
\def\xbtoxch{\xb\to \xc h^-}
\def\xbtoxckpipi{\xb\to\xc K^-\pi^+\pi^-}
\def\xbtoxck{\xb\to\xc K^-}

\def\efake{\epsilon_{misID}(p,p_T,\eta)}

\def\btodhhh{B\to D + 3h}
\def\bstodspipipi{\bar{B}^0_s\to D_s^+\pi^-\pi^+\pi^-}
\def\bstodskpipi{\bar{B}^0_s\to D_s^+K^-\pi^+\pi^-}
\def\btodpipipi{\bar{B}^0\to D^+\pi^-\pi^+\pi^-}
\def\btodkpipi{\bar{B}^0\to D^+K^-\pi^+\pi^-}
\def\btodstarkpipi{\bar{B}^0\to D^{*+}K^-\pi^+\pi^-}
\def\btodzeropipipi{B^-\to D^0\pi^-\pi^+\pi^-}
\def\btodzerokpipi{B^-\to D^0 K^-\pi^+\pi^-}
\def\btodstarzeropipipi{B^-\to D^{*0}\pi^-\pi^+\pi^-}
\def\btodstarzerokpipi{B^-\to D^{*0} K^-\pi^+\pi^-}

\def\btodstarpipipi{\bar{B}^0\to D^{*+}\pi^-\pi^+\pi^-}
\def\LbtoLcpipipi{\Lambda_b^0\to\Lambda_c^+\pi^-\pi^+\pi^-}
\def\LbtoLckpipi{\Lambda_b^0\to\Lambda_c^+ K^-\pi^+\pi^-}
\def\LbtoLcpi{\Lambda_b^0\to\Lambda_c^+\pi^-}
\def\LbtoLck{\Lambda_b^0\to\Lambda_c^+K^-}
\def\btodh{B\to Dh}
\def\bstodspi{\bar{B}_s^0\to D_s^+\pi^-}
\def\bstodsk{B_s^0\to D_s^{\mp}K^{\pm}}
\def\btodpi{\bar{B}^0\to D^+\pi^-}
\def\btodk{\bar{B}^0\to D^+K^-}
\def\btodstarpi{\bar{B}^0\to D^{*+}\pi^-}
\def\btodzeropi{B^{-}\to D^0\pi^{-}}
\def\btodzerok{B^-\to\Dz K^-}
\def\btodzerobark{B^-\to \Dzb K^-}
\def\eff{\epsilon}
\def\btodzerokstar{B^{-}\to D^0K^{*0}}

\def\ifb{\rm fb^{-1}}
\def\ipb{\rm pb^{-1}}

\def\delB{\delta_B}
\def\delD{\delta_D}
\def\Xsm{K^-\pi^+\pi^-}
\def\Xsp{K^+\pi^-\pi^+}
\def\Dcp{D_{CP\pm}}

\def\fscf{f^*_{CF}}
\def\fscs{f^*_{CS}}
\def\fgpi{f_{\Dz\gamma/\Dz\pi}}

\def\rw{r_w}
\def\fcore{f_{\rm core}}
\def\rstat{{\rm stat}}
\def\rsyst{{\rm syst}}
